import subprocess

'''
calls sim with no input
'''
def test():
    # Simple command without capturing output
    #subprocess.run(['ls', '-l'])

    # Command with output capture
    #result = subprocess.run(['ls', '-l'], capture_output=True, text=True)
    #print(result.stdout)  # Prints the command output
    return False